//Initial State
const initialState = { };
const ClassesReducer= (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default ClassesReducer;
