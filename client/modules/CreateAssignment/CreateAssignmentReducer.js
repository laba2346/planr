
// Initial State
const initialState = {  };
const CreateAssignmentReducer= (state = initialState, action) => {
    switch (action.type) { 

        default:
            return state;
    }
};

/* Selectors */
// Export Reducer`
export default CreateAssignmentReducer;
